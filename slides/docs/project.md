**Implémenter Flow Matching sur un jeu 2D (mixtures/circles). Choisir une “probability path” simple entre bruit et données, entraîner un réseau à régresser le champ de vitesse conditionnel, puis échantillonner en intégrant une dynamique discrétisée (Euler). Comparer à RealNVP sur les mêmes données.**

**Objectifs**

Formaliser l’objectif Flow Matching (régression de champ).

Implémenter un chemin simple bruit→données et l’échantillonnage.

Évaluer NLL (si dispo) ou métriques 2D (MMD, visuels).

Comparer à un flow discret (RealNVP) en complexité/qualité.
